import torch

mask=[]

mask_conv1 = torch.zeros(64,3,7,7).cuda()
mask_layer1_basic1 = torch.zeros(64,64,3,3).cuda()
mask_layer1_basic2 = torch.zeros(64,64,3,3).cuda()
mask_layer1_basic3 = torch.zeros(64,64,3,3).cuda()
mask_layer1_basic4 = torch.zeros(64,64,3,3).cuda()
mask_layer1_basic5 = torch.zeros(64,64,3,3).cuda()
mask_layer1_basic6 = torch.zeros(64,64,3,3).cuda()
mask_layer2_basic1 = torch.zeros(128,64,3,3).cuda()
mask_layer2_basic2 = torch.zeros(128,128,3,3).cuda()
mask_layer2_basic3 = torch.zeros(128,128,3,3).cuda()
mask_layer2_basic4 = torch.zeros(128,128,3,3).cuda()
mask_layer2_basic5 = torch.zeros(128,128,3,3).cuda()
mask_layer2_basic6 = torch.zeros(128,128,3,3).cuda()
mask_layer2_basic7 = torch.zeros(128,128,3,3).cuda()
mask_layer2_basic8 = torch.zeros(128,128,3,3).cuda()
mask_layer3_basic1 = torch.zeros(256,128,3,3).cuda()
mask_layer3_basic2 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic3 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic4 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic5 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic6 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic7 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic8 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic9 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic10 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic11 = torch.zeros(256,256,3,3).cuda()
mask_layer3_basic12 = torch.zeros(256,256,3,3).cuda()
mask_layer4_basic1 = torch.zeros(512,256,3,3).cuda()
mask_layer4_basic2 = torch.zeros(512,512,3,3).cuda()
mask_layer4_basic3 = torch.zeros(512,512,3,3).cuda()
mask_layer4_basic4 = torch.zeros(512,512,3,3).cuda()
mask_layer4_basic5 = torch.zeros(512,512,3,3).cuda()
mask_layer4_basic6 = torch.zeros(512,512,3,3).cuda()
mask_layer2_downsample = torch.zeros(64,128,3,3).cuda()
mask_layer3_downsample = torch.zeros(128,256,3,3).cuda()
mask_layer4_downsample = torch.zeros(256,512,3,3).cuda()


mask.append(mask_conv0_weight)
mask.append(mask_conv1_weight)
mask.append(mask_conv2_weight)
mask.append(mask_conv3_weight)
mask.append(mask_conv4_weight)
mask.append(mask_conv5_weight)
mask.append(mask_conv6_weight)
mask.append(mask_conv7_weight)
mask.append(mask_conv8_weight)
mask.append(mask_conv9_weight)
mask.append(mask_conv10_weight)
mask.append(mask_conv11_weight)
mask.append(mask_conv12_weight)
mask.append(mask_fc0_weight)
mask.append(mask_fc1_weight)
mask.append(mask_fc2_weight)

torch.save(mask, 'mask_null_res34.dat')

